{
  "name": "plug-for-nodejs-buildpack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "echo 'Start Plug buildpack'",
    "build": " echo 'Before Build Plug buildpack' && npm install --prefix client && npm run build --prefix client && echo 'After Build Plug buildpack'"
  }
}
